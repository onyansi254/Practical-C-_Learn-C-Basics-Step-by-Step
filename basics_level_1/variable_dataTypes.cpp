#include <iostream>

int main()
{
    int user_age = 20;
    double temperature = 22.5; // it is called float used to store decimal numbers with more digits
    bool is_alive = false; // this is a boolean that takes two choices true or false(1 or 0)
    std::string user_name = "Kelv";
    
    std::cout << user_age << std::endl;
    std::cout << temperature << std::endl;
    std::cout << is_alive << std::endl;
    std::cout << user_name << std::endl;

    return 0;
}