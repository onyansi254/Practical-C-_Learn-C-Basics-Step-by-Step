#include <iostream>
using namespace std;

int main()
{
    cout << boolalpha;   // when used the boolean values are represented as text not as (1 or 0)
    
    cout << (1==1) << endl;  //for comparison operators !=, ==, <, <=, >, >=

    //cout << true << endl;
    //cout << false << endl;

    cout << ("hello" == "hallo") << endl;
    return 0;
}
